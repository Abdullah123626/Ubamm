import { View, Text } from 'react-native'
import React from 'react'
import MainStake from './Src/Navigation/MainStake';

export default function App () {
  return (
    <View style={{flex:1}} >
      <MainStake/>
    </View>
  )
}